# ⛔ 일을 시작하기 전에 — 넥서스부터 읽는다

**이 저장소만 보고 일하지 말 것.** 부장님의 기본 규칙·작업절차·기억의 정본은
여기가 아니라 **`Jeguevara2/nexus`** 에 있다. 넥서스를 안 읽고 일하면
규칙을 모르는 채로 일하게 된다.

---

## 세션을 시작하면 이것부터 이행한다

묻지 말고 바로 한다. 이미 돼 있으면 건너뛴다.

### 웹 · 아이패드 · 원격 세션일 때

이 세션은 부장님 PC 가 아니라 **빈 클라우드 컨테이너**다. 넥서스가 붙어 있지 않다.

1. `list_repos` 로 `Jeguevara2/nexus` 를 확인한다
2. `add_repo(owner="Jeguevara2", repo="nexus")`
3. `git clone --depth 1 https://github.com/Jeguevara2/nexus /workspace/nexus`
   — 한 번만, 넉넉한 시간(10분)을 주고
4. `register_repo_root(owner="jeguevara2", repo="nexus", directory="/workspace/nexus")`
   → 넥서스의 `CLAUDE.md` 가 다음 턴에 자동으로 올라온다

### PC 세션일 때

`nexus.ps1` 이 `~/.claude/CLAUDE.md` 를 이미 맞춰 두었을 것이다. 최신화만 한다.

```powershell
git -C D:\ClaudeLocal\nexus pull
```

### 붙인 다음 (공통)

1. 넥서스 **`CLAUDE.md`** → 기본 규칙(답변 방식·토큰 절약·계정 규칙)
2. 그 일에 필요한 **`workflows/`** 만 골라 읽는다 — **통째로 읽지 말 것**(토큰 낭비)
3. **`memory/MEMORY.md`** 목차를 보고 필요한 줄만 펼친다
4. **`state/지시함_새지시.md`** 를 본다 — 대기 건이 있으면 **그게 1순위**이고,
   진짜 지시함으로 옮긴 뒤 그 칸을 비운다

---

## 왜 이 파일이 여기 있나 — 2026-08-14 에 드러난 구멍

넥서스는 `nexus.ps1` 이 `$env:USERPROFILE\.claude\CLAUDE.md` 로 **복사**해서 퍼진다.
그건 **Windows PC 에서만** 동작한다.

웹·아이패드 세션은 부장님 PC 에서 도는 게 아니라 저장소 하나만 받아 새로 켜진
클라우드 컨테이너다. `$env:USERPROFILE` 이 아예 없으니 그 복사본이 닿지 않는다.
그래서 README 가 말하는 「집 PC · 회사 PC · 아이패드 어디서 일하든」에서
**아이패드·웹은 실제로는 빠져 있었다.**

넥서스 README 의 「대화를 시작하면 먼저 확인할 것」이라는 지시도 **넥서스 안에** 있어서,
넥서스를 이미 읽고 있어야만 볼 수 있다. 닭과 달걀이다.

**세션에 붙는 저장소마다 이 파일을 두는 것이, 웹 세션이 넥서스를 찾는 유일한 길이다.**

> ⚠ **이 파일은 기본 가지(main)에 있어야 효력이 있다.** 새 세션은 기본 가지를 받아 켜지므로,
> 딴 가지에만 있으면 다음 세션도 똑같이 넥서스를 못 찾는다.

> 📌 **새 저장소를 만들 때마다** 이 파일을 같이 넣을 것. 넣지 않은 저장소로 세션이 열리면
> 그 세션은 넥서스를 모르는 채로 일한다.
